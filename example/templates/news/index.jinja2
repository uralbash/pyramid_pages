{% extends 'pyramid_pages/base.jinja2' %}

{% block content %}
  {{ context.node.date }}<div class="page-title">{{ context.node.name }}</div>
  <div class="content">{{ context.node.description or '' }}</div>
{% endblock %}
